<UserControl x:Class="UdemyTranscriptExtractor.Controls.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MaxWidth="400">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Colors.xaml"/>
                <ResourceDictionary Source="/Themes/TextStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Border x:Name="ToastBorder"
            Background="{StaticResource SecondaryBackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="16"
            Opacity="0"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TranslateTransform x:Name="SlideTransform" X="400"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.4" Color="Black"/>
        </Border.Effect>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Icon -->
            <Border Grid.Column="0"
                    Width="40" Height="40"
                    CornerRadius="20"
                    Margin="0,0,12,0"
                    VerticalAlignment="Top">
                <Border.Background>
                    <SolidColorBrush x:Name="IconBackground" Color="#3FB950"/>
                </Border.Background>
                
                <Viewbox Width="24" Height="24">
                    <Canvas Width="24" Height="24">
                        <!-- Success Icon (Checkmark) -->
                        <Path x:Name="SuccessIcon"
                              Data="M9,16.17L4.83,12l-1.42,1.41L9,19L21,7l-1.41-1.41L9,16.17z"
                              Fill="White"
                              Visibility="Visible"/>
                        
                        <!-- Error Icon (X) -->
                        <Path x:Name="ErrorIcon"
                              Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41z"
                              Fill="White"
                              Visibility="Collapsed"/>
                        
                        <!-- Info Icon (i) -->
                        <Path x:Name="InfoIcon"
                              Data="M11,9h2V7h-2V9z M12,20c-4.41,0-8-3.59-8-8s3.59-8,8-8s8,3.59,8,8S16.41,20,12,20z M12,2C6.48,2,2,6.48,2,12 s4.48,10,10,10s10-4.48,10-10S17.52,2,12,2z M11,17h2v-6h-2V17z"
                              Fill="White"
                              Visibility="Collapsed"/>
                        
                        <!-- Warning Icon (!) -->
                        <Path x:Name="WarningIcon"
                              Data="M1,21h22L12,2L1,21z M13,18h-2v-2h2V18z M13,14h-2v-4h2V14z"
                              Fill="White"
                              Visibility="Collapsed"/>
                    </Canvas>
                </Viewbox>
            </Border>
            
            <!-- Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText"
                          Text="Success"
                          Style="{StaticResource SubtitleTextStyle}"
                          FontSize="14"
                          Margin="0,0,0,4"/>
                
                <TextBlock x:Name="MessageText"
                          Text="Operation completed successfully!"
                          Style="{StaticResource BodyTextStyle}"
                          FontSize="12"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
