<UserControl x:Class="UdemyTranscriptExtractor.Controls.FloatingActionButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="64" Height="64">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Colors.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Ellipse x:Name="ButtonBackground"
                 Width="64" Height="64"
                 Fill="{StaticResource AccentGradientBrush}"
                 Cursor="Hand"
                 RenderTransformOrigin="0.5,0.5">
            <Ellipse.RenderTransform>
                <ScaleTransform x:Name="ScaleTransform"/>
            </Ellipse.RenderTransform>
            <Ellipse.Effect>
                <DropShadowEffect x:Name="Shadow"
                                BlurRadius="16"
                                ShadowDepth="4"
                                Opacity="0.4"
                                Color="#7C3AED"/>
            </Ellipse.Effect>
        </Ellipse>
        
        <Viewbox Width="28" Height="28">
            <Canvas Width="24" Height="24" x:Name="IconCanvas"
                    RenderTransformOrigin="0.5,0.5">
                <Canvas.RenderTransform>
                    <RotateTransform x:Name="IconRotate"/>
                </Canvas.RenderTransform>
                
                <!-- Download/Extract Icon -->
                <Path x:Name="ExtractIcon" 
                      Data="M19,9h-4V3H9v6H5l7,7L19,9z M5,18v2h14v-2H5z"
                      Fill="White"
                      Visibility="Visible"/>
                
                <!-- Checkmark Icon -->
                <Path x:Name="CheckmarkIcon"
                      Data="M9,16.17L4.83,12l-1.42,1.41L9,19L21,7l-1.41-1.41L9,16.17z"
                      Fill="White"
                      Visibility="Collapsed"
                      RenderTransformOrigin="0.5,0.5">
                    <Path.RenderTransform>
                        <ScaleTransform x:Name="CheckmarkScale"/>
                    </Path.RenderTransform>
                </Path>
            </Canvas>
        </Viewbox>
    </Grid>
</UserControl>
